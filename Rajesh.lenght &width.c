// To find the square of a number
#include<stdio.h>
void main()
{
	int length,width;
	printf("enter a length & width");
	scanf("%d%d",&length,&width);
	printf("area of the rectangle is %d\n",length*width);
	printf("perimeter of the rectangle is %d",2*(length+width));
	
}
